import React from 'react';

const WeatherMessage = React.createClass({
    render: function() {
        const message = this.
        return (
            <p>This is a simple message</p>
        )
    }
})